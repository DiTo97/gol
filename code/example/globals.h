#ifndef GLOBAL_H
#define GLOBAL_H 

const int DEFAULT_VIS_INTERVAL = 60;
const int DEFAULT_TIMESTEPS = 1000;
const int DEFAULT_SIZE_ROWS = 105;
const int DEFAULT_SIZE_COLS = 105;
const int DEFAULT_INIT_PROB = 0.5;

enum {
  DEAD = 0,
  ALIVE = 1
};
#endif